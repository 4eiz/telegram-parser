# 🇷🇺 Telegram Search Parser

**Языки / Languages:**
🇷🇺 Русский — этот файл  
🇬🇧 English — [README_EN.md](README_EN.md)

**Telegram Search Parser** — это инструмент для массового поиска каналов и чатов в Telegram по ключевым словам с поддержкой:

* нескольких аккаунтов (каждый со своим `.json` + `.session`),
* прокси,
* глубокой генерации поисковых запросов (deep-search),
* фильтрации результатов на *чаты* и *каналы*,
* логирования и обработки ошибок,
* автоматического сохранения результатов.

Подходит для OSINT, маркетинга, аналитики и сегментации целевой аудитории.

---

## 🔥 Возможности

| Возможность                  | Описание                                                                      |
| ---------------------------- | ----------------------------------------------------------------------------- |
| Поиск по ключевым словам     | Ищет каналы и чаты по словам/фразам из `queries.txt`.                         |
| Глубокий поиск (Deep-Search) | Автоматически расширяет запросы (`reddit → reddit a, reddit b, reddit 0..9`). |
| Классификация результатов    | Корректно различает **каналы (broadcast)** и **чаты/мегагруппы (megagroup)**. |
| Мульти-аккаунтность          | Каждый аккаунт используется в собственном `.session` и `.json`.               |
| Поддержка прокси             | Прокси берутся из `proxy.txt`, могут отличаться для каждого аккаунта.         |
| Обход ограничений Telegram   | Случайные задержки и backoff защита от блокировок.                            |
| Автосохранение результатов   | Результаты пишутся в `results_channels.txt` и `results_chats.txt`.            |

---

## 📂 Требования

* **Python 3.10+**
* Аккаунты Telegram (каждая пара файлов вида:
  `111111111.json` + `111111111_telethon.session`)
* Если используется прокси — рабочие SOCKS5 / HTTP прокси.

---

## 🗂️ Структура проекта

```
project/
├─ main.py                  # точка запуска
├─ queries.txt              # ключевые запросы
├─ proxy.txt                # список прокси (опционально)
├─ results_channels.txt     # результаты каналов
├─ results_chats.txt        # результаты чатов
├─ Accounts/                # сюда кладем *.json + *_telethon.session
└─ tgparser/
   ├─ accounts.py
   ├─ client.py
   ├─ parser.py
   ├─ deepsearch.py
   ├─ proxies.py
   ├─ config.py
   └─ ...
```

---

## ⚙️ Установка

```bash
git clone https://github.com/USERNAME/REPO.git
cd REPO

python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac

pip install -r requirements.txt
```

---

## 🔧 Настройка

1. Переименуй `.env.example` → `.env`
2. В `.env` можно управлять глубиной поиска:

```env
DEEP_SEARCH=1         # включить/отключить глубокий поиск
LIMIT=200             # общий лимит итоговых результатов
```

3. В папку `Accounts/` клади пары:

```
123456789.json
123456789_telethon.session
```

4. В `queries.txt` пиши ключевые слова:

```
Reddit
Crypto
Новости
```

---

## 🚀 Запуск

```bash
python main.py
```

---

## 📄 Результаты

После выполнения появятся файлы:

```
results_channels.txt
results_chats.txt
```

Формат строк:

```
Название | Количество участников | Ссылка
```